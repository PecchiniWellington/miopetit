export interface IBrand {
  id: string;
  name: string;
  image: string | null;
}

export interface IPathology {
  id: string;
  name: string;
}

export interface IProtein {
  id: string;
  name: string;
}
